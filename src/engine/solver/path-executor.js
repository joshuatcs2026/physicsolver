import { solveDirect, buildSubstitution, checkDirectSolve } from './direct-solver.js';
import { inferResultUnit } from '../validation/dimensional-analysis.js';
export function executeSolvePath({path,equations,knowns,constants,variableIndex}){const equationById=new Map(equations.map(eq=>[eq.id,eq])),values=new Map(knowns),trace=[];for(const step of path.steps){if(values.has(step.target))continue;const equation=equationById.get(step.equationId);if(!equation)return{status:'error',message:`Missing equation ${step.equationId}`,trace};const missing=(equation.targetMap[step.target]||[]).filter(id=>!values.has(id)&&!constants.has(id));if(missing.length)return{status:'blocked',message:`Step ${step.target} still needs ${missing.join(', ')}`,trace};try{const substitution=buildSubstitution(equation,step.target,values,constants),result=solveDirect(equation,step.target,values,constants),check=checkDirectSolve({equation,target:step.target,knowns:values,result,variableIndex}),variable=variableIndex.byId.get(step.target),unit=inferResultUnit(variable);values.set(step.target,{value:result,unit,derived:true});trace.push({equationId:equation.id,target:step.target,targetName:variable?.name||step.target,expression:equation.expression,substitution,result,unit,check});if(!check.ok)return{status:'warning',message:'A calculated step produced warnings.',trace,values};}catch(error){return{status:'error',message:error.message,trace};}}const finalTarget=path.steps.at(-1)?.target,final=values.get(finalTarget);return{status:final?'solved':'blocked',result:final?.value,unit:final?.unit||null,trace,values};}
