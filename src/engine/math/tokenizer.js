export function tokenize(input){const out=[];let i=0;while(i<input.length){const c=input[i];if(/\s/.test(c)){i++;continue}if(/[0-9.]/.test(c)){let s='';while(i<input.length&&/[0-9.eE+-]/.test(input[i]))s+=input[i++];if(!Number.isFinite(Number(s)))throw new Error('Invalid number: '+s);out.push({type:'number',value:Number(s)});continue}if(/[A-Za-z_]/.test(c)){let s='';while(i<input.length&&/[A-Za-z0-9_]/.test(input[i]))s+=input[i++];out.push({type:'name',value:s});continue}if('+-*/^()='.includes(c)){out.push({type:c,value:c});i++;continue}throw new Error('Unexpected character: '+c)}return out}