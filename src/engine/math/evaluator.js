export function evaluate(n,v={}){switch(n.type){case'num':return n.value;case'var':if(!Number.isFinite(v[n.name]))throw new Error('Missing variable: '+n.name);return v[n.name];case'neg':return-evaluate(n.arg,v);case'bin':{const a=evaluate(n.left,v),b=evaluate(n.right,v);const r=n.op==='+'?a+b:n.op==='-'?a-b:n.op==='*'?a*b:n.op==='/'?a/b:Math.pow(a,b);if(!Number.isFinite(r))throw new Error('Non-finite result');return r}case'fn':{const a=evaluate(n.arg,v),f={sqrt:Math.sqrt,sin:Math.sin,cos:Math.cos,tan:Math.tan,asin:Math.asin,acos:Math.acos,atan:Math.atan,ln:Math.log,log:Math.log10,abs:Math.abs}[n.name],r=f(a);if(!Number.isFinite(r))throw new Error('Invalid function domain');return r}default:throw new Error('Unknown node')}}
export function variables(n,s=new Set()){if(n.type==='var')s.add(n.name);if(n.arg)variables(n.arg,s);if(n.left)variables(n.left,s);if(n.right)variables(n.right,s);return s}